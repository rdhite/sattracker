# Current Task: Phase 1 - Setup

1. Initialize a Python FastAPI backend structure.
2. Create a uv project with: `fastapi`, `uvicorn`, `skyfield`, `rasterio`, `numpy`.
3. Create a basic endpoint `/health` that returns `{"status": "ok"}`.
4. Create a dummy endpoint `/predict` that accepts `{lat, lon}` and returns a mock satellite pass list.